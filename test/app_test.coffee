request = require 'request'

describe "iCalledIt Setup", ->
  it "uses Mocha for tests", -> true    
  it "uses CoffeeScript for most of the things", -> true
  it "uses requireJs for script loading", -> true
  it "uses Backbone", -> true